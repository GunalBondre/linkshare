{
	"name": "linktree",
	"version": "1.0.0",
	"main": "server.ts",
	"license": "MIT",
	"dependencies": {
		"concurrently": "^8.2.0",
		"nodemon": "^3.0.1"
	},
	"scripts": {
		"backend": "cd backend && yarn build && yarn start",
		"client": "cd frontend && yarn dev",
		"dev": "concurrently \"yarn backend\" \"yarn client\""
	}
}
